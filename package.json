{
  "name": "tmk-consumer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pull": "git pull origin master && npm install && git submodule update --init --recursive",
    "update-submodule": "git submodule update --remote && git add . && git commit -m 'Updated submodule in the consumer: $1'"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.11.2"
  }
}
